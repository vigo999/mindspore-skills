---
description: Build MindSpore GPU operators using CUDA
---

# MindSpore GPU Builder

Build GPU operators with CUDA kernels.

Load the `gpu-builder` skill and follow its workflow.

## Key Locations

- Kernels: `mindspore/ccsrc/plugin/device/gpu/kernel/`
- CUDA files: `*.cu`, `*.cuh`
- Tests: `tests/st/ops/gpu/`
