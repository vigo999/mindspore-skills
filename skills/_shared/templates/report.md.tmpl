# Summary
- Skill: `{{ skill_name }}`
- Run ID: `{{ run_id }}`
- Status: `{{ status }}`
- Duration: `{{ duration_sec }}s`

# What
- Task: `{{ task_name }}`
- Target: `{{ target_name }}`

# How
- Route: `{{ route_name }}`
- Key steps: `{{ step_summary }}`

# Verify
- Command(s): `{{ verify_commands }}`
- Result: `{{ verify_result }}`

# Artifacts
- `{{ artifact_list_or_none }}`

# Environment
- MindSpore: `{{ mindspore_version }}`
- CANN: `{{ cann_version }}`
- Driver: `{{ driver_version }}`
- Python: `{{ python_version }}`
- Platform: `{{ platform }}`
- Git commit: `{{ git_commit }}`

# Logs
- `logs/run.log`
- `logs/build.log`
- `logs/verify.log`

# Next
- `{{ next_actions }}`
