{
  "skill": "{{ skill_name }}",
  "run_id": "{{ run_id }}",
  "parameters": {
    "target": "{{ target }}",
    "mode": "{{ mode }}",
    "extra": "{{ extra }}"
  },
  "masked_keys": [
    "token",
    "password",
    "secret"
  ]
}
