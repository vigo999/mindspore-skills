# Patching Rules

Patch generation should prefer:

- minimal edits
- preserved baseline behavior by default
- config deltas instead of hidden hardcoded behavior when possible
- explicit notes for unresolved or uncertain details
