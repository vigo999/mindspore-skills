# Integration Planning

Use this reference when mapping a feature into an existing model codebase.

Focus on:

- target modules and configs
- whether config-only integration is possible
- the smallest safe patch scope
- baseline constraints that should remain fixed
