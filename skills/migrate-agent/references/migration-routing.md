# Migration Routing

`migrate-agent` is the top-level migration entry.

Choose one route:

- `hf-transformers`
- `hf-diffusers`
- `generic-pytorch-repo`

Use explicit user requirement first, then workspace evidence and source-library
identity.
